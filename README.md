# 供養ポスト

誰にも言えなかったことを書いて、海に流す。
データはどこにも保存されません。

## ローカル起動

```bash
npm install
npm run dev
# http://localhost:5173
```

## ビルド（静的ファイル出力）

```bash
npm run build
# dist/ に出力されます
# Cloudflare Pages / Vercel / Netlify にそのままデプロイ可
```
